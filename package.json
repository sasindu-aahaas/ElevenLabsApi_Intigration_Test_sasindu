{
  "name": "elevenlabs-mern-test",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "npm run dev --workspace client",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
